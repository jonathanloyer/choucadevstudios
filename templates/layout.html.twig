<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">

    <title>{% block title %}ChoucaDev Studios{% endblock %}</title>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description"
          content="ChoucaDev Studios — Création de sites vitrines, web design, développement web & mobile, SEO et solutions digitales en Île-de-France.">

    {# Favicons #}
    <link rel="apple-touch-icon" sizes="180x180" href="{{ asset('favicon/apple-touch-icon.png') }}">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('favicon/favicon-32x32.png') }}">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('favicon/favicon-16x16.png') }}">
    <link rel="manifest" href="{{ asset('favicon/site.webmanifest') }}">
    <link rel="shortcut icon" href="{{ asset('favicon/favicon.ico') }}">

    <link rel="stylesheet" href="{{ asset('build/app.css') }}">
  </head>

  <body class="bg-slate-950 text-slate-100 min-h-screen flex flex-col">

    {# ==========================
       HEADER
       ========================== #}
    <header class="border-b border-slate-800 bg-slate-900/80 backdrop-blur">
      <div class="max-w-6xl mx-auto px-4 py-3 flex items-center justify-between gap-6">

        {# Logo + lien vers accueil (toujours à gauche) #}
        <a href="{{ path('home') }}" class="flex items-center gap-3 shrink-0">
          <img src="{{ asset('images/logo.png') }}" alt="ChoucaDev Studios Logo"
               class="w-10 h-10 sm:w-11 sm:h-11 object-contain drop-shadow-[0_0_12px_rgba(0,200,255,0.6)]">

          <span class="flex flex-col leading-tight">
            <span class="text-base sm:text-lg font-semibold tracking-wide">
              ChoucaDev <span class="text-cyan-400">Studios</span>
            </span>
            <span class="text-[11px] sm:text-xs text-slate-400">
              Studio digital &amp; web
            </span>
          </span>
        </a>

        {# Bloc de droite : nav + compte + burger #}
        <div class="flex items-center gap-6">

          {# MENU DESKTOP (>= md) #}
          <nav class="hidden md:block text-sm">
            <ul class="flex gap-6 items-center">
              <li>
                <a href="{{ path('home') }}" class="hover:text-cyan-300 transition">
                  Accueil
                </a>
              </li>
              <li>
                <a href="{{ path('services') }}" class="hover:text-cyan-300 transition">
                  Services
                </a>
              </li>
              <li>
                <a href="{{ path('contact') }}" class="hover:text-cyan-300 transition">
                  Contact
                </a>
              </li>

              {# ➕ Lien Tableau de bord (seulement si connecté) #}
              {% if app.user %}
                <li>
                  <a href="{{ path('app_dashboard_redirect') }}" class="hover:text-cyan-300 transition">
                    Tableau de bord
                  </a>
                </li>
              {% endif %}
            </ul>
          </nav>

          {# LIENS COMPTE (>= md) #}
          <nav class="hidden md:block text-xs sm:text-sm">
            <ul class="flex items-center gap-3">
              {% if app.user %}
                <li>
                  <a href="{{ path('profile') }}" class="hover:text-cyan-300 transition">
                    Mon compte
                  </a>
                </li>
                <li>
                  <a href="{{ path('app_logout') }}"
                     class="px-3 py-1 rounded-full border border-slate-600 hover:border-cyan-400 hover:text-cyan-300 transition">
                    Déconnexion
                  </a>
                </li>
              {% else %}
                <li>
                  <a href="{{ path('app_register') }}" class="hover:text-cyan-300 transition">
                    Inscription
                  </a>
                </li>
                <li>
                  <a href="{{ path('app_login') }}"
                     class="px-3 py-1 rounded-full border border-cyan-400 text-cyan-300 hover:bg-cyan-500 hover:text-slate-950 transition">
                    Se connecter
                  </a>
                </li>
              {% endif %}
            </ul>
          </nav>

          {# BOUTON BURGER (mobile < md) #}
          <button id="nav-toggle"
                  class="md:hidden inline-flex items-center justify-center p-2 rounded-md border border-slate-700 text-slate-200 hover:border-cyan-400 hover:text-cyan-300"
                  aria-label="Ouvrir le menu"
                  aria-expanded="false">
            <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none"
                 xmlns="http://www.w3.org/2000/svg">
              <path d="M4 6H20M4 12H20M4 18H20"
                    stroke="currentColor" stroke-width="2" stroke-linecap="round" />
            </svg>
          </button>
        </div>
      </div>

      {# MENU MOBILE (< md) : nav + compte #}
      <nav id="mobile-nav" class="md:hidden hidden border-t border-slate-800 bg-slate-900/95">
        <div class="max-w-6xl mx-auto px-4 py-3 text-sm">
          <ul class="flex flex-col gap-2">
            <li>
              <a href="{{ path('home') }}" class="block py-1 hover:text-cyan-300 transition">
                Accueil
              </a>
            </li>
            <li>
              <a href="{{ path('services') }}" class="block py-1 hover:text-cyan-300 transition">
                Services
              </a>
            </li>
            <li>
              <a href="{{ path('contact') }}" class="block py-1 hover:text-cyan-300 transition">
                Contact
              </a>
            </li>

            {# ➕ Lien Tableau de bord en mobile aussi (si connecté) #}
            {% if app.user %}
              <li>
                <a href="{{ path('app_dashboard_redirect') }}" class="block py-1 hover:text-cyan-300 transition">
                  Tableau de bord
                </a>
              </li>
            {% endif %}

            <li class="mt-2 border-t border-slate-700 pt-2">
              {% if app.user %}
                <a href="{{ path('profile') }}" class="block py-1 hover:text-cyan-300 transition">
                  Mon compte
                </a>
                <a href="{{ path('app_logout') }}" class="block py-1 hover:text-cyan-300 transition">
                  Déconnexion
                </a>
              {% else %}
                <a href="{{ path('app_register') }}" class="block py-1 hover:text-cyan-300 transition">
                  Inscription
                </a>
                <a href="{{ path('app_login') }}" class="block py-1 hover:text-cyan-300 transition">
                  Se connecter
                </a>
              {% endif %}
            </li>
          </ul>
        </div>
      </nav>
    </header>

    {# ==========================
       CONTENU
       ========================== #}
    <main class="flex-1">
      {% block body %}{% endblock %}
    </main>

    {# ==========================
       FOOTER
       ========================== #}
    <footer class="border-t border-slate-800 bg-slate-900/90 mt-8">
      <div class="max-w-6xl mx-auto px-4 py-6 text-xs sm:text-[13px] text-slate-400
                  flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
        <div>
          &copy; {{ "now"|date("Y") }} ChoucaDev Studios — Tous droits réservés.
        </div>
        <div class="text-slate-500">
          Site développé avec Symfony &amp; Tailwind CSS.
        </div>
      </div>
    </footer>

    {# ==========================
       SCRIPT MENU BURGER
       ========================== #}
    <script>
      document.addEventListener('DOMContentLoaded', function () {
        const toggle = document.getElementById('nav-toggle');
        const mobileNav = document.getElementById('mobile-nav');

        if (!toggle || !mobileNav) return;

        toggle.addEventListener('click', () => {
          const isHidden = mobileNav.classList.contains('hidden');
          if (isHidden) {
            mobileNav.classList.remove('hidden');
            toggle.setAttribute('aria-expanded', 'true');
          } else {
            mobileNav.classList.add('hidden');
            toggle.setAttribute('aria-expanded', 'false');
          }
        });

        // Quand on repasse en écran >= md, on ferme le menu mobile
        window.addEventListener('resize', () => {
          if (window.innerWidth >= 768) { // md
            mobileNav.classList.add('hidden');
            toggle.setAttribute('aria-expanded', 'false');
          }
        });
      });
    </script>

  </body>
</html>
